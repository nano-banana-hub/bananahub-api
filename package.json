{
  "name": "bananahub-api",
  "version": "0.1.0",
  "description": "Cloudflare Worker API for BananaHub install count tracking",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
